# Ruff — CI lint: once kritik hatalar (tum repo formatini zorlamaz)
line-length = 120
target-version = "py311"

[lint]
# Syntax/undefined name seviyesinde kal — mevcut stile uyumlu
select = ["E9", "F63", "F7", "F82", "F811", "F821"]
ignore = []

[lint.per-file-ignores]
"tests/*" = ["E402"]
